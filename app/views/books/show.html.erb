<p id="notice"><%= notice %></p>

<h1><%= t('views.headings.show') %></h1>

<p>
<strong><%= t('views.labels.title') %></strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t('views.labels.memo') %></strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t('views.labels.author') %></strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t('views.labels.picture') %></strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t('views.buttons.edit'), edit_book_path(@book) %> |
<%= link_to t('views.buttons.back'), books_path %>
